import { Descriptions } from "antd";

export const Lista = ({ items }) => {

    const generarItems = () => {
        
    }

    return (
        <Descriptions
            bordered
            items={items}
            className="descriptions-container"
        />
    )
}